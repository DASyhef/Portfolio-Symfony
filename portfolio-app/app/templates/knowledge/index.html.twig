{% extends 'base.html.twig' %}

{% block title %}Hello KnowledgeController!{% endblock %}

{% block body %}
{#
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/src/Controller/KnowledgeController.php'|file_link(0) }}">src/Controller/KnowledgeController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/templates/knowledge/index.html.twig'|file_link(0) }}">templates/knowledge/index.html.twig</a></code></li>
    </ul>
</div>
#}


<style>
.knowledge { background-image: url("{{ asset('build/Portfolio/BG/bel-arbre-au-milieu-champ-couvert-herbe-limite-arbres-arriere-plan_181624-29267.png') }}");}
</style>


<section class="knowledge" id="Knowledge">
    <h1 class="title">Connaissances</h1>

    <div class="knowledgecontainer">
    <div class="code inrow">
        {% for paragraf in paragrafs %}
            <div class="divskills">{{ paragraf.paragraf }}</div>
        {% endfor %}
    </div>
    <div class="toolscode inrow">
        {% for paragraf in paragrafs %}
            <div class="divskills">{{ paragraf.paragraf }}</div>
        {% endfor %}
    </div>
    <div class="othertools inrow">
        {% for paragraf in paragrafs %}
            <div class="divskills">{{ paragraf.paragraf }}</div>
        {% endfor %}
    </div>
    <div class="os inrow">
        {% for paragraf in paragrafs %}
            <div class="divskills">{{ paragraf.paragraf }}</div>
        {% endfor %}
    </div> 
    <a href="#ultrafooter"><img class="arrow invert" src="{{ asset('build/Portfolio/icons/circle-arrow-down-solid.svg') }}" alt="arrow"></a>

</section>

{% endblock %}
