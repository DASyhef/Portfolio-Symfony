{% extends 'base.html.twig' %}

{% block title %}SkillsManager index{% endblock %}

{% block body %}
    <h1>SkillsManager index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Skills</th>
                <th>IMG</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for skills_manager in skills_managers %}
            <tr>
                <td>{{ skills_manager.id }}</td>
                <td>{{ skills_manager.Skills }}</td>
                <td>{{ skills_manager.IMG }}</td>
                <td>
                    <a href="{{ path('app_skills_manager_show', {'id': skills_manager.id}) }}">show</a>
                    <a href="{{ path('app_skills_manager_edit', {'id': skills_manager.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_skills_manager_new') }}">Create new</a>
{% endblock %}
