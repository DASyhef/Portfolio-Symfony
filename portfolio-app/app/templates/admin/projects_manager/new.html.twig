{% extends 'baseAdmin.html.twig' %}

{% block title %}New ProjectsManager{% endblock %}

{% block body %}
<style>
    .admin { background-image: url("{{ asset('build/Portfolio/BG/forest-old-growth-nature-green-wallpaper.jpg') }}")}
</style>

<section class="admin">
    <div class="panel">
    <h1>Create new ProjectsManager</h1>

    {{ include('admin/projects_manager/_form.html.twig') }}

    <a href="{{ path('app_projects_manager_index') }}">back to list</a>
    </div>
</section>
{% endblock %}
