{% extends 'base.html.twig' %}

{% block title %}ProjectsManager{% endblock %}

{% block body %}
    <h1>ProjectsManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projects_manager.id }}</td>
            </tr>
            <tr>
                <th>Projects</th>
                <td>{{ projects_manager.Projects }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ projects_manager.name }}</td>
            </tr>
            <tr>
                <th>IMG</th>
                <td>{{ projects_manager.IMG }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ projects_manager.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_projects_manager_index') }}">back to list</a>

    <a href="{{ path('app_projects_manager_edit', {'id': projects_manager.id}) }}">edit</a>

    {{ include('admin/projects_manager/_delete_form.html.twig') }}
{% endblock %}
