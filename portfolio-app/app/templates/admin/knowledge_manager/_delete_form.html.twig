<form method="post" action="{{ path('app_knowledge_manager_delete', {'id': knowledge_manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ knowledge_manager.id) }}">
    <button class="btn">Delete</button>
</form>
